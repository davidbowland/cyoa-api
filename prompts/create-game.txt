# {"anthropicVersion":"bedrock-2023-05-31","maxTokens":115000,"model":"us.anthropic.claude-sonnet-4-6","temperature":0.6,"topK":500}

<context>
You are creating a choose-your-own-adventure game. The best choices should lead to the best outcome. Some choices can be puzzles the player needs to figure out or remember. You are creating the outline for the game that will be used in subsequent LLM prompts to generate the narrative and choices.
</context>
<instructions>
Come up with the outline for a choose-your-own-adventure story based on the theme and sample of the inspiration words provided.

- Create a story title
  - Image description to be passed to an LLM to generate an image
  - Keep it concise but vivid (1-2 sentences)
  - NEVER include text or numbers in the image
- Create a story description
  - 2-3 sentences
  - Written in the imperative ("Explore the world of..." or "Locate the hidden treasure of...")
- Create a cast of characters (up to 5 + narrator)
  - Name, themed with the content
    - NEVER put anything but a name -- this value is displayed to the player
  - Image description to be passed to an LLM to generate an image
    - Focus on the setting, atmosphere, and key visual elements
    - Keep it concise but vivid (1-2 sentences)
    - NEVER include text or numbers in the image
  - Voice actor
    - Female: "Danielle", "Joanna", "Ruth", or "Salli"
    - Male: "Matthew" or "Stephen"
    - ONLY THOSE VOICE ACTORS ARE AVAILABLE
    - EACH VOICE ACTER CAN ONLY BE USED ONCE
  - One character should be the narrator named "Narrator" (no image required)
    - The narrator needs a voice actor
- Create a list of possible inventory items
  - Create the number of items requested (examples: keys, keycards, tools, identification / credentials, the item being sought like a painting if the player is stealing a painting, etc)
  - Name of each item, themed with content
    - NEVER put anything but a name -- this value is displayed to the player
  - Image description to be passed to an LLM to generate an image
    - Focus on a physical representation of the item alone (size, important features, specific type, etc)
    - Keep it concise but vivid (1-2 sentences)
    - NEVER include text or numbers in the image
- Create a loss condition
  - Create a resource name, themed with the content
    - NEVER put anything but a name -- this value is displayed to the player
  - The resource needs to be countable (minutes, coins, etc) or a percent (% lamp oil remaining, etc) NOT something general like time (instead do hours, days, etc)
  - Resource image description to be passed to an LLM to generate an image
    - Focus on a physical representation of the item alone
    - Keep it concise but vivid (1-2 sentences)
    - NEVER include text or numbers in the image
  - If the condition is `accumulate`, wrong answers add resources
  - If the condition is `reduce`, wrong answers reduce resources
  - Generate a starting amount
    - There must be AT LEAST minimumResourceRange between the starting amount and loss threshold
  - Determine a loss threshold
    - Reaching the threshold makes the player lose the game
- Create an outline of the story
  - Generate the overall narrative for the cyoa story, 2-3 paragraphs
  - Do NOT generate choices, those will be generated later from your narrative
  - Describe in 1-2 sentences what happens if the player wins / succeeds
  - Describe in 1-2 sentences what happens if the player loses
  - Use the second person -- "you" perspective
- NOT ALL inspiration words will be used. THINK ABOUT which are best and should be incorporated into the story. You can use inspiration words directly, use related words, or use random words with similar letters, sounds, or meanings to inspiration words
- ALL generated content MUST be inoffensive and not emmotionally charged

Think deeply about the most engaging story that is suitable for cyoa. What is the overall idea? What makes the most sense as a loss condition? Who is it important to meet and who should voice them?
</instructions>
<output>
Output your thinking in <thinking> tags followed by valid JSON with the keys with NOTHING ELSE (double-check that your JSON is valid with \n instead of newlines and " escaped):

{
  "title": "string",
  "description": "string",
  "titleImageDescription": "string",
  "resourceName": "string",
  "resourceImageDescription": "string",
  "startingResourceValue": number,
  "lossResourceThreshold": number,
  "outline": "string",
  "characters": [
    { "name": "string", "imageDescription": "string", "voice": "string" }
  ],
  "inventory": [
    { "name": "string", "imageDescription": "string" }
  ]
}
</output>

<input>
${context}
</input>
